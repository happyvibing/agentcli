{
  "name": "@happyvibing/agentcli",
  "version": "0.2.0",
  "repository": {
    "type": "git",
    "url": "git+ssh://git@github.com/happyvibing/agentcli.git"
  },
  "description": "AgentCLI \u2014 call MCP servers and OpenAPI APIs from the command line. One CLI, any tool backend.",
  "type": "module",
  "bin": {
    "agentcli": "bin/agentcli.js"
  },
  "files": [
    "bin",
    "dist/src",
    "dist/package.json",
    "skills",
    "README.md",
    "!dist/src/**/*.d.ts",
    "!dist/src/**/*.map"
  ],
  "keywords": [
    "cli",
    "mcp",
    "model-context-protocol",
    "agent",
    "ai",
    "agentcli",
    "tool-runtime"
  ],
  "license": "MIT",
  "author": "happyvibing",
  "publishConfig": {
    "access": "public"
  },
  "engines": {
    "node": ">=20.10"
  },
  "scripts": {
    "clean": "rm -rf dist",
    "build": "rm -rf dist && tsc",
    "start": "node bin/agentcli.js",
    "test": "node --test dist/test/*.test.js",
    "prepublishOnly": "rm -rf dist && tsc && node --test dist/test/*.test.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "commander": "^12.1.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "@types/node": "^22.0.0"
  },
  "packageManager": "pnpm@10.28.0"
}
